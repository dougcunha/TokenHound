<Window x:Class="TokenHound.App.UI.Windows.NotchWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:TokenHound.App.UI.Controls"
        mc:Ignorable="d"
        Title="TokenHound"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False"
        SizeToContent="WidthAndHeight"
        ResizeMode="NoResize"
        WindowStartupLocation="Manual">

    <Grid Margin="12">
        <Border Background="#121214"
                BorderBrush="#27272A"
                BorderThickness="1"
                CornerRadius="22"
                Padding="6,4"
                SnapsToDevicePixels="True">
            <Border.Effect>
                <DropShadowEffect Color="#000000"
                                  BlurRadius="16"
                                  ShadowDepth="3"
                                  Opacity="0.45"
                                  Direction="270" />
            </Border.Effect>

            <ItemsControl ItemsSource="{Binding Rings}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <controls:ProviderRing UsedFraction="{Binding UsedFraction}"
                                               ProviderBadge="{Binding ProviderBadge}"
                                               Status="{Binding Status}"
                                               IsBusy="{Binding IsBusy}"
                                               Margin="2,0" />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Border>
    </Grid>

</Window>
